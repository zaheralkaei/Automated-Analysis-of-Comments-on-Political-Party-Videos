# ─────────────────────────────────────────────────────────────────────────────
# Automated Analysis of Stance, Rhetorical Techniques, and Divisiveness in YouTube Comments on Political Party Videos — Pipeline Dependencies
# Python >= 3.10
#
# Install all:          pip install -r requirements.txt
# 
# GPU PyTorch (CUDA 12): replace the torch line with the appropriate wheel URL
#                        https://pytorch.org/get-started/locally/
# ─────────────────────────────────────────────────────────────────────────────

# Environment
python-dotenv>=1.0.0

# Steps 01-02: Data collection (YouTube API + transcripts)
google-api-python-client>=2.100.0
isodate>=0.6.1
youtube-transcript-api>=1.0.0

# Step 06: Topic modelling (BERTopic)
bertopic>=0.16.0
sentence-transformers>=2.7.0
nltk>=3.8.0

# Step 09: Manual annotation UI
streamlit>=1.32.0

# Steps 10-11: LLM annotation (Anthropic / Claude)
anthropic>=0.25.0

# Step 17: LLM evaluation (OpenRouter — OpenAI-compatible API)
openai>=1.14.0

# Progress bars
tqdm>=4.66.0

# Core data processing
numpy>=1.26.0
pandas>=2.1.0
pyarrow>=15.0.0
scipy>=1.12.0

# Steps 14-19: Machine learning
scikit-learn>=1.4.0
xgboost>=2.0.3
joblib>=1.3.0

# Steps 14, 16, 19: BERT / GRoBERTa
# CPU-only build — replace with a CUDA wheel for GPU training:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.2.0
transformers>=4.40.0
sentencepiece>=0.2.0        

# Steps 18-20: Visualisation
matplotlib>=3.8.0
seaborn>=0.13.0

# Step 19: Explainability
shap>=0.45.0

# Token-level BERT attributions via captum LayerIntegratedGradients (step 19)
captum>=0.7.0
